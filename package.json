{
  "name": "framekit-workspace",
  "private": true,
  "packageManager": "pnpm@11.14.0",
  "engines": {
    "node": ">=22.13.0",
    "pnpm": ">=11.14.0"
  },
  "scripts": {
    "dev": "pnpm --filter @mauriciodmo/framekit build && pnpm --filter studio dev",
    "lint": "pnpm -r --if-present lint",
    "test": "pnpm -r --if-present test",
    "typecheck": "pnpm -r --if-present typecheck",
    "build": "pnpm -r --if-present build",
    "test:e2e": "playwright test",
    "smoke:tarballs": "node tooling/smoke-tarballs.mjs",
    "smoke:docker": "node tooling/smoke-docker.mjs",
    "sync:skills": "node tooling/sync-skills.mjs",
    "check:runtime": "node tooling/check-runtime-contract.mjs",
    "prepare": "husky"
  },
  "devDependencies": {
    "@playwright/test": "^1.51.1",
    "eslint-plugin-import": "^2.32.0",
    "eslint-plugin-n": "^18.3.0",
    "eslint-plugin-promise": "^7.3.0",
    "globals": "^17.12.0",
    "husky": "^9.1.7"
  }
}
